import { StyleSheet } from "react-native";

export const GLOGABL_STYLES = StyleSheet.create({
  input: {
    borderWidth: 1,
    borderColor: "#ccc",
    color: "white",
    width: "100%",
    padding: 12,
    borderRadius: 8,
    marginBottom: 32,
  },
  error: {
    color: "red",
    fontSize: 12,
    marginBottom: 16,
  },
});
